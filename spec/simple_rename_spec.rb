require 'spec_helper'

describe SimpleRename do
  it 'has a version number' do
    expect(SimpleRename::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
